[Unit]
# A monitor whose own failures are silent is the failure it exists to prevent.
# OnFailure fires when the CHECK breaks; the check's own alerts are about the
# thing being checked, and those two facts need saying separately.
OnFailure=pcoin-alert@%n.service
